package models.enums

/**
 * Enums necesarios para las razas
 */
enum class RazasBondadosas(val value:Int) {
    PELOSOS(1),SUREÑOS_BUENOS(2),ENANOS(3),NUMENOREANOS(4),ELFOS(5)
}

enum class RazasMalvadas(val value:Int){
    SUREÑOS_MALOS(1),ORCOS(2),GOBLINS(3),HUARGOS(4),TROLLS(5)
}